<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TEMPEST</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>
<body class="flex items-center justify-center min-h-screen bg-gray-100">

  <div class="container mx-auto p-4 max-w-md bg-white rounded-lg shadow-lg">
    <h1 class="text-2xl font-bold mb-4 text-center">TEMPEST</h1>

    <form class="mb-4" id="weather-form" method="POST" action="/weather">
      <div class="flex">
        <input class="flex-grow p-2 border border-gray-300 rounded-l-md" 
               id="city-input" 
               name="city" 
               placeholder="Enter city name" 
               required 
               type="text"/>
        <button class="p-2 bg-blue-500 text-white rounded-r-md" type="submit">
          <i class="fas fa-search"></i>
        </button>
      </div>
    </form>

    <!-- Weather Result -->
    <% if (city) { %>
      <div id="weather-result" class="mt-4 p-4 bg-gray-100 border border-gray-300 rounded-md shadow">
        <h2 class="text-xl font-semibold" id="city-name">CITY: <%= city %></h2>
        <p class="text-gray-700" id="temperature"><b>TEMPERATURE:</b> <%= temp %></p>
        <p class="text-gray-600" id="weather-description"><b>WEATHER DESCRIPTION:</b> <%= weather %></p>
        <img id="weather-icon" src="<%= icon %>" alt="Weather Icon" class="mx-auto mt-2">
      </div>
    <% } %>

    <!-- Error Message -->
    <% if (error) { %>
      <div class="mt-4 p-4 bg-red-100 text-red-700 border border-red-300 rounded-md shadow">
        <p><%= error %></p>
      </div>
    <% } %>

  </div>

</body>
</html>
